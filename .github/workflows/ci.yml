name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  placeholder:
    name: CI placeholder
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Explain pending implementation
        run: echo "Full FlexPass CI is implemented in Phase 12."
